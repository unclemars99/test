name: E000-H ordered physical composition

on:
  push:
    branches: [ master ]
    paths:
      - 'src/e000f_fixed_vs_process_token.py'
      - 'src/e000h_ordered_physical_composition.py'
      - 'experiments/E000H_ordered_physical_composition.md'
      - '.github/workflows/e000h.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-e000h:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas scipy scikit-learn tabulate kagglehub
          pip install torch --index-url https://download.pytorch.org/whl/cpu

      - name: Run E000-H
        run: python src/e000h_ordered_physical_composition.py

      - name: Commit reproducible results
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin master || true
          git add results/e000h || true
          if git diff --cached --quiet; then
            echo "No result changes"
          else
            git commit -m "Record E000-H ordered physical composition results"
            git push
          fi
